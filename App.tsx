import React from 'react';
import {
  SafeAreaView,
  ScrollView,
  StatusBar,
  StyleSheet,
  Text,
  useColorScheme,
  View,
} from 'react-native';
import './constants/firebase';

function App(): JSX.Element {
  return (
    <SafeAreaView>
      <View>
        <Text>fgaddfasfas</Text>
      </View>
    </SafeAreaView>
  );
}

const styles = StyleSheet.create({});

export default App;
